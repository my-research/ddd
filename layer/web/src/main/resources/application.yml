spring:
  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ddd-layer
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
